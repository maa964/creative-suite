# 実装計画: ロードマップの作成

## 1. 現状分析

- **README.md** を参照し、現在のバージョン（v0.7.0）と完了したマイルストーン（v0.6.0）の情報を抽出。
- **目標説明.txt** から、各アプリ（Photo/Paint, Video, Vector, Launcher）の設計思想と実装状況を把握。
- **pyproject.toml** で現在の依存関係（PySide6, huggingface-hub, AIスタック等）を確認。
- `apps/` フォルダの構成から、共通基盤（`apps/common`）や AI モジュール（`apps/ai`）の進捗を判断。

## 2. 内容の構成案

- **ロードマップ概要**: 本プロジェクトが目指す方向（OSSベースのクリエイティブ・スイート）
- **ステータス表示**: 現在のバージョンと主要機能
- **完了済みフェーズ**: v0.6.0、v0.7.0 の振り返り
- **将来フェーズ**: v1.0.0〜v2.0.0 に向けた技術的、機能的目標
- **短期開発優先事項**: 直近の開発で見込まれる課題とタスク

## 3. 作成

- ルートディレクトリに `roadmap.md` として保存。
- ユーザー指示に従い、日本語で、かつ読みやすい Markdown 形式で記述。

## 4. 検証

- 既存の `README.md` に記載されていたロードマップと矛盾がないか。
- `build.ps1` や `claude.md` との整合性が保たれているか（特にロードマップ優先の原則）。
