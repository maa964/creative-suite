# 修正内容の確認: ロードマップの作成

## 変更点の概要

プロジェクト全体の進捗と今後の計画を可視化するため、ルートディレクトリに `roadmap.md` を作成しました。

## 実装内容

- **README.md**、**目標説明.txt**、**pyproject.toml** から情報を集約し、現在のステータス（v0.7.0）を記述。
- **v0.6.0 (Video/Vector基本実装)** と **v0.7.0 (AI Studio統合)** を「完了済み」として計上。
- 今後の主要マイルストーンを定義：
  - **v1.0.0**: MSI インストーラー、マルチプラットフォーム、正式安定版。
  - **v1.1.0**: 効果・トランジション（Video/Vector の進化）。
  - **v1.2.0**: アプリ間のデータパイプライン（統合の深化）。
  - **v1.3.0**: プラグインストアの本番化、SDK公開。
  - **v2.0.0**: クラウド同期、共同編集。
- 短期的な優先事項として、ビルドの安定化、UIコンポーネントの共通化、プラグインAPIの仕様確定を明記。

## 動作確認 (セルフチェック)

- `roadmap.md` がプロジェクトルートに存在することを確認しました。
- 各バージョンの内容が、既存のコード構成（`apps/`, `launcher/`, `server/`, `plugins/`）と整合しています。
- 日本語で記述され、開発の指針として活用可能な内容であることを確認しました。
